---
import Layout from '../layouts/Layout.astro';
import ProjectCard from '../components/ProjectCard.astro';
import projects from '../data/projects.json';

const site = Astro.site?.toString().replace(/\/$/, '') ?? 'https://www.ggnagarkar.com';
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'CollectionPage',
  name: 'Projects and Products by GG Nagarkar',
  description: 'Products and projects across enterprise AI, AIOps, cloud automation, and BMC monitoring, log analytics, observability, and ITSM integration.',
  url: `${site}/projects`,
  hasPart: projects.map((project) => ({
    '@type': 'CreativeWork',
    name: project.name,
    description: project.summary,
    url: project.url ?? `${site}/projects`
  }))
};
---
<Layout
  title="Projects and Products | GG Nagarkar"
  description="Projects and products by GG Nagarkar across Applied AI Consulting, OpsRabbit, 7Targets, BMC Software, OpexSoftware, REAN Cloud, TCS, and patent-backed cloud automation."
  canonicalPath="/projects"
  jsonLd={jsonLd}
  breadcrumbs={[
    { name: 'Home', item: `${site}/` },
    { name: 'Projects', item: `${site}/projects` }
  ]}
>
  <h1 class="mb-4 text-3xl font-semibold text-slate-900">Projects</h1>
  <p class="mb-5 max-w-3xl text-slate-700">
    Work spans enterprise AI and cloud automation as well as earlier BMC product work in monitoring, observability, operational log analytics, and ITSM-connected incident workflows. For deeper architecture notes, outcomes, scale, and lessons learned, see <a href="/built" class="underline">Things I've Built</a>.
  </p>
  <div class="grid gap-4 md:grid-cols-2">
    {projects.map((project) => <ProjectCard project={project} />)}
  </div>
</Layout>
